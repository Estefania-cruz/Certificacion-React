const activo = true;
/*let mensaje ='';
if (!activo) {
    mensaje = ' activo';
} else{
    mensaje = 'no activo';
}
console.log(mensaje);*/

const mensaje =(activo ) ? 'activo'  : 'no activo';
console.log(mensaje);

const men = (activo) && 'activo';
console.log(men);