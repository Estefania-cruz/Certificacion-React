console.log('HOLA MUNDO');

//VARIABLES Y CONSTANTES
const nombre = 'fernando';
let apellido = 'cristobal';
// let sirve para cambiar los valores o los datos
// pero si los datos no se cambian mucho se utiliza CONST
let valordado = 5 ;

console.log(nombre, apellido, valordado);

//cuando se hace una condicion y tiene el mismo nombre de una variable que esta arriba como valor dado no importa por que el la condicion se ocupa como  si no se hubiera ocupado asi 
//que no afecta a la condicion y se puede utilizar de cualquier manera.
if (true) {
    let valordado = 8;
    console.log(valordado);
}

//var no se debe se usar por que choca y causa problemas